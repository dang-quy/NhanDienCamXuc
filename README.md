# Speech Emotion Recognition App

Ứng dụng nhận diện cảm xúc giọng nói (4 lớp: vui, buồn, giận dữ, ngạc nhiên) sử dụng mô hình LSTM + MFCC.

## Cách chạy

1. Copy mô hình `speech_emotion_lstm_4classes.keras` vào thư mục `model/`
2. Cài đặt thư viện:
   ```bash
   pip install -r requirements.txt